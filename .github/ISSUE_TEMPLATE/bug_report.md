---
name: Bug report
about: Something in `forge` is wrong
title: "[bug] "
labels: bug
---

**What happened**
A clear description. What command did you run, what did you expect, what did
you actually see?

**Reproduction**
```bash
# the exact commands
```

**Environment**
- `forge --version`:
- OS + arch (e.g. `macOS 14.5 arm64`, `Ubuntu 24.04 x86_64`):
- `ollama --version`:
- Model in use:
- Hardware (RAM / VRAM / GPU model):

**Logs**
Run with `forge --verbose` and paste the relevant output. Redact any secrets.

**`forge.toml`** (if relevant)
```toml
```
